apiVersion: v1
kind: Service
metadata:
  name: {{ include "mediamarkt.fullname" . }}
  labels:
    {{- include "mediamarkt.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mediamarkt.selectorLabels" . | nindent 4 }}
